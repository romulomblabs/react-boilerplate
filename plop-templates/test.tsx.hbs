import { render } from "@testing-library/react";

import {{pascalCase name}} from "../{{pascalCase name}}";


test("renders {{pascalCase name}} correctly", () => {
  const { getByText } = render(<{{pascalCase name}}>My {{pascalCase name}}</{{pascalCase name}}>);
  const linkElement = getByText(/My {{pascalCase name}}/i);
  expect(linkElement).toBeInTheDocument();
});
